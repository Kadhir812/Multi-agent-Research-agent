# Leave unset for local dev — Vite's dev proxy forwards /api to localhost:8000.
# Set this to your deployed backend URL when deploying the frontend (e.g. on Vercel).
VITE_API_BASE_URL=https://multi-agent-research-agent-nu.vercel.app
